const BUILD_DEV = 'build:dev';
const BUILD_PROD = 'build:prod';
const BUILD_SRC = 'build:src';
const BUILD_STYLE = 'build:style';
const TSC = 'tsc';
const BUILD = 'build';
const DEV = 'dev';
const SERVE = 'serve';
const args = [BUILD_DEV, BUILD_PROD, BUILD_SRC, TSC, BUILD, DEV, SERVE, BUILD_STYLE];

module.exports = {
    args,
    BUILD_DEV,
    BUILD_PROD,
    BUILD_SRC,
    TSC,
    BUILD,
    DEV,
    SERVE,
    BUILD_STYLE,
};
